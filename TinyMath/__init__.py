from Number1 import Number

new_number = Number()
c = new_number.isprime(8)  # when we have a return type we need to capture the return type.
                           # here 'C' is the return type
print(c)

d= new_number.isArmstrong(0)
print(d)